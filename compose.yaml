services:
  opencode-web:
    build: .
    image: opencode-web:dev
    ports:
      - "8080:8080"
    volumes:
      - ./workspace:/home/node/workspace
      - ./data/opencode-state/config:/home/node/.config/opencode
      - ./data/opencode-state/data:/home/node/.local/share/opencode
